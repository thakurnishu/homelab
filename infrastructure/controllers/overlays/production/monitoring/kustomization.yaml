apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/monitoring
  - grafana-db.yaml
  - grafana-user.yaml

patches:
  - path: kube-prometheus-stack-release.yaml

namespace: monitoring
