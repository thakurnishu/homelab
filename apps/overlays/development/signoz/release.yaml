apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: signoz
spec:
  chart:
    spec:
      sourceRef:
        namespace: signoz
  values:
    clickhouse:
      installCustomStorageClass: true

