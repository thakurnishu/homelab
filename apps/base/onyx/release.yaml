apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: onyx
spec:
  interval: 5m
  timeout: 20m
  releaseName: onyx
  chart:
    spec:
      chart: onyx-stack
      version: '0.3.*'
      sourceRef:
        kind: HelmRepository
        name: onyx
